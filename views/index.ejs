<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-700 w-full rounded-2xl outline-none text-white" type="text" name="title"
                    placeholder="title goes here">
                <textarea
                    class="block px-5 py-2 bg-zinc-700 w-full rounded-2xl outline-none text-white resize-none mt-2"
                    name="details" id="" placeholder="write your task details"></textarea>
                <input class="mt-2 bg-blue-500 text-white px-4 py-2 rounded-2xl text-lg" type="submit"
                    value="Create Task">
            </form>
        </div>

        <div class="tasks p-10 flex gap-2 flex-wrap justify-center">

            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task w-71 px-3 py-4 rounded-2xl bg-zinc-700">
                        <h1 class="text-white text-2xl"><%= val  %></h1>
                        <a class="text-blue-600 inline-block mt-2" href="">Read more</a>
                    </div>
                <% }) %>
            <% } else { %>
                <h3 class="text-white text-3xl">No Tasks Yet</h3>
            <% } %>
                          
        </div>


    </div>
</body>

</html>